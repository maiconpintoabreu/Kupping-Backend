apiVersion: v1
kind: Service
metadata:
  labels:
    app: kupping-backend
  name: kupping-backend-service
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: kupping-backend
  sessionAffinity: None
  type: ClusterIP